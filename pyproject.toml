[project]
name = "kiwipulse"
version = "0.1.0"
description = "Periodic system health monitor with Discord alerts"
requires-python = ">=3.14"
dependencies = [
    "python-dotenv>=1.2.2",
    "requests>=2.33.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.8",
]
